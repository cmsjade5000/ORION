# Memory - 2026-04-07

## ORION Voice Update
- Updated ORION persona to be stronger opinions, no corporate hedging, brevity mandatory, humor allowed, call out dumb ideas, swearing when appropriate. Committed `77cce80`.

## AEGIS Noise Fix
- Deployed updated `aegis-sentinel` to suppress routine Tailscale online/active churn (`AEGIS_TAILSCALE_STATUS_ALERT:=0`).
- Hardened `deploy_aegis_remote.sh` to fail loudly instead of hanging.

## ORION Judgment Layer MVP
- Built `scripts/orion_judgment_layer.py` that consumes incident bundle, scores signals, outputs verdict with `alert`/`digest`/`log-only`.
- Added policy wiring (`ops/judgment-policy.v1.json`).
- Wired into local maintenance schedule path; writes `tmp/orion_judgment_latest.json` and `tasks/NOTES/orion-judgment.md`, plus historical artifacts in `eval/history/`.
- Verified dry-run execution; current verdict `ok`, recommendation `log-only`.

## Cron Backbone Enablement
- Enabled safe maintenance jobs: `assistant-agenda-refresh`, `assistant-task-loop`, `assistant-inbox-notify` (healthy instance), `orion-ops-bundle`.
- Left broken duplicate inbox notifier disabled.
- Kickstarted most jobs; verified exit codes `0` and healthy logs.

## Cron Cleanup (In Progress)
- Removed `assistant-inbox-notify` (broken duplicate) from cron.
- Added scheduled `orion-judgment-layer` cron job.
- Discovered `orion-ops-bundle` runs but logs missing commands (`openclaw`, `codex`) in its PATH; fix not yet verified.
- Started stale one-shot reminder cleanup; at least one removed.

## Next Steps
- Verify and fix `orion-ops-bundle` LaunchAgent PATH.
- Complete stale one-shot job cleanup.
- Decide whether to enable `orion-session-maintenance` after review.
- Potentially enable judgment-layer cron permanently after stabilization.
