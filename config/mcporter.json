{
  "mcpServers": {
    "codex": {
      "command": "codex",
      "args": [
        "mcp-server"
      ],
      "description": "Codex MCP server via stdio"
    },
    "primeta": {
      "url": "https://primeta.ai/mcp",
      "description": "Optional Primeta avatar presentation layer for ORION via hosted MCP + OAuth"
    }
  },
  "imports": []
}
