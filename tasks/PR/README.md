# PR Review Packets

This folder is used for local, lightweight PR review Task Packets generated by:
- `scripts/pr_poll.sh`

By default, generated files are ignored by git (`tasks/PR/PR-*.md`) to avoid noise.

